<h1>Listing bills</h1>

<table>
  <thead>
    <tr>
      <th>Billed at</th>
      <th>Print date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @bills.each do |bill| %>
      <tr>
        <td><%= bill.billed_at %></td>
        <td><%= bill.print_date %></td>
        <td><%= link_to 'Anzeigen', bill %></td>
        <td><%= link_to 'Angezeigtes Datum edieren', edit_bill_path(bill) %></td>
        <td><%= link_to 'Löschen', bill, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>