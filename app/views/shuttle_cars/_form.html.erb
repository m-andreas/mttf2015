<%= form_for(@shuttle_car) do |f| %>
  <% if @shuttle_car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shuttle_car.errors.count, "error") %> prohibited this shuttle_car from being saved:</h2>

      <ul>
      <% @shuttle_car.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :car_brand, "Fahrzeugmarke" %><br>
    <%= f.text_field :car_brand %>
  </div>
  <div class="field">
    <%= f.label :car_type, "Fahrzeugtype" %><br>
    <%= f.text_field :car_type %>
  </div>
  <div class="field">
    <%= f.label :registration_number, "Kennzeichen" %><br>
    <%= f.text_field :registration_number %>
  </div>
  <div class="actions">
    <%= f.submit "Fahrzeug speichern" %>
  </div>
<% end %>
